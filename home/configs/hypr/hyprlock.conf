$font = JetBrainsMono Nerd Font

# Color $variables, written by switch-profile/toggle-variant
# (apply_hyprlock_colors). Absolute path: hyprlang does not expand ~.
source = /home/rupan/.config/hypr/profile-colors.conf

general {
    hide_cursor = true
    ignore_empty_input = true
    immediate_render = true
}

animations {
    enabled = true
    bezier = smooth, 0.22, 1, 0.36, 1
    animation = fadeIn, 1, 4, smooth
    animation = fadeOut, 1, 4, smooth
    animation = inputFieldDots, 1, 2, smooth
}

background {
    monitor =
    path = screenshot
    blur_passes = 4
    blur_size = 7
    noise = 0.008
    contrast = 0.92
    brightness = 0.55
    vibrancy = 0.12
    vibrancy_darkness = 0.2
}

shape {
    monitor =
    size = 100%, 100%
    color = rgba(05070b66)
    rounding = 0
    position = 0, 0
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:1000] date +"%-I:%M"
    color = $fg
    font_size = 92
    font_family = $font
    position = 0, 172
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:60000] date +"%A, %B %-d"
    color = $muted
    font_size = 22
    font_family = $font
    position = 0, 96
    halign = center
    valign = center
}

input-field {
    monitor =
    size = 360, 54
    outline_thickness = 0
    rounding = 10

    outer_color = rgba(00000000)
    inner_color = rgba(00000000)
    font_color = $fg
    check_color = $fg
    fail_color = $error

    font_family = $font
    placeholder_text = <span></span>
    fail_text = $PAMFAIL
    check_text = Checking...
    capslock_color = rgba(ebcb8be6)
    swap_font_color = true
    fade_on_empty = false
    dots_size = 0.24
    dots_spacing = 0.22
    shadow_passes = 0
    shadow_size = 0

    position = 0, -104
    halign = center
    valign = center
}
