import Quickshell

// Alternative entrypoint — run with: qs -p ~/.config/quickshell-xhisper/main.qml
// The canonical entrypoint for `qs -p ~/.config/quickshell-xhisper` is shell.qml.
ShellRoot {
    XhisperOverlay {}
}
